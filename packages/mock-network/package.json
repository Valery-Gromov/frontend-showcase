{
    "name": "@frontend-showcase/mock-network",
    "version": "0.1.0",
    "description": "Dev-only fetch-compatible mock transport with latency, race conditions, and partial-success support.",
    "private": true,
    "type": "module",
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "exports": {
        ".": "./src/index.ts"
    },
    "scripts": {
        "build": "echo build",
        "typecheck": "tsc --noEmit -p tsconfig.json",
        "test": "vitest run",
        "lint": "echo lint"
    }
}

