{
  "name": "@frontend-showcase/hooks",
  "version": "0.1.0",
  "description": "Reusable React hooks for data-heavy UI: URL query state, debounced value, request sequencing, external change detection.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo build",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "echo lint"
  },
  "peerDependencies": {
    "react": "^18.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.24"
  }
}
